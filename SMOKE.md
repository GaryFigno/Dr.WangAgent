# Dr.Wang Agent 手测清单（打包后）

运行：`dist\Dr.Wang\Dr.Wang.exe` 或源码 `aih gui`。

## 安装与托盘
- [ ] 双击 `Dr.Wang.exe` 能开窗（需 WebView2）
- [ ] 点窗口 X → 进托盘，不退出进程
- [ ] 托盘「显示主窗口」可恢复；「退出程序」才退出

## 首次配置（未配置时）
- [ ] 顶部出现 onboarding 步骤条
- [ ] 设置里能加账号并落盘；重启后账号还在
- [ ] 指定 main 角色后 onboarding 消失，可对话

## 语言
- [ ] 设置 → 界面语言切到 English / 日本語，侧栏与按钮立刻变
- [ ] 重启后语言保持

## 对话稳态
- [ ] 工具运行中点打断，再提问 → 不出现 tool_calls HTTP 400
- [ ] Plan 模式可点顶栏 PLAN 退出，且不粘住
- [ ] Write/Edit 出现待审条；回滚能恢复文件
- [ ] yolo +「自动接受」开启时不弹待审条

## Quest / 检索
- [ ] 开始 Quest → Verify 失败后条带显示 blocked
- [ ] 点「从断点续」会自动开一轮继续
- [ ] 文件面板内容搜索能命中并预览
- [ ] `@` 补全能按扩展名过滤（若前端传了 ext）

## Bash 审阅
- [ ] 让 agent 用 Bash 写文件 → 待审条出现侧写文件

## 行情（需 `market.enabled`）
- [ ] 报价 / 30 日 K 线
- [ ] MA 回测出现权益曲线
- [ ] 加提醒后查报价可触发 toast（价到条件时）

## 多窗口
- [ ] 侧栏 ⧉ 弹出选目录，新进程独立会话
